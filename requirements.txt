# Core web
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
jinja2==3.1.4

# Database
sqlmodel==0.0.22
alembic==1.14.0
psycopg2-binary==2.9.10

# Config / secrets
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Auth — Google OAuth (professor sign-in). itsdangerous signs the session cookie,
# so there is no server-side session store and the app stays Autoscale-friendly.
authlib==1.3.2
itsdangerous==2.2.0

# Data (Moodle export parsing)
pandas==2.2.3
openpyxl==3.1.5
odfpy==1.4.1

# Browser automation (local runner only — Moodle/SEGA adapters)
playwright==1.49.0

# AI (in-app analyst + SUSAI) — Gemini
google-genai==1.58.0

# HTTP client (testing, Meta API calls)
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
